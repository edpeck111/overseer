"""Configuration loader (paths, ports, model names, feature flags).

Sprint 0 stub. Sprint 4 (static-shell discipline) is the first sprint
that loads anything meaningful here; until then ``legacy_server``
hard-codes its own constants.
"""

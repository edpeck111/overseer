"""OVERSEER v3 backend package.

The v2 Flask app currently lives at the repo root in legacy_server.py.
During Sprint 0 this package exists as scaffolding with a compatibility
shim in server/app.py. Subsequent sprints split legacy_server.py into
the per-domain modules in server/modules/.

See docs/04-IMPLEMENTATION-PLAN.md for the sprint roadmap.
"""

__version__ = "3.0.0-dev0"

"""SQLite connection + migrations runner.

Sprint 0 stub. Migrations live in ``server/migrations/`` (created in a
later sprint) as numbered ``NNN_*.sql`` files. ``migrate()`` is called
from :func:`server.app.create_app` once that factory replaces the
compatibility shim.
"""

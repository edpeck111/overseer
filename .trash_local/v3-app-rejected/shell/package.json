{
  "name": "overseer-shell",
  "version": "3.0.0-dev",
  "description": "OVERSEER v3 — static client shell",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "size": "node esbuild.config.mjs && du -h dist/overseer.* | sort"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}
